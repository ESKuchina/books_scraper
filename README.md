# 📚 Books Scraper

Система для автоматического сбора информации о книгах  
с сайта [Books to Scrape](https://books.toscrape.com).

---

## 🎯 Цель проекта

Разработать инструмент для парсинга каталога книг с сайта Books to Scrape,  
с возможностью сохранения данных, ускоренного сбора (многопоточность),  
автоматического ежедневного запуска и проверки корректности с помощью автотестов.

---

## ⚙️ Основные возможности

- Получение данных об одной книге (`get_book_data`)
- Сбор всех книг с 50 страниц каталога (`scrape_books`)
- Опциональная многопоточность для ускорения (`use_threads=True`)
- Сохранение результата в файл `artifacts/books_data.txt`
- Автоматический запуск каждый день в 19:00 (`run_scheduler`)
- Полное покрытие тестами (`pytest`)
- Код соответствует стандартам **PEP 8** и **PEP 257**

---

## 🧩 Структура проекта

books_scraper/
├── artifacts/
│ └── books_data.txt
├── notebooks/
│ └── HW_03_python_ds_2025.ipynb
├── scraper.py
├── README.md
├── requirements.txt
├── tests/
│ └── test_scraper.py
└── venv/


---

## 🧰 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/<your_username>/books_scraper.git
cd books_scraper

### 2. Создание и активация виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate        # macOS / Linux

### 3. Установка зависимостей
```bash
pip install -r requirements.txt


---

## 🚀 Режимы запуска

### 1. Последовательный (безопасный)

```bash
python -c "import scraper; scraper.scrape_books(is_save=True, use_threads=False)"


или просто:

```bash
python scraper.py

### 2. Ускоренный (многопоточный)

Использует до 8 потоков, ускоряет работу в 3–6 раз:

```bash
python -c "import scraper; scraper.scrape_books(is_save=True, use_threads=True)"

⚠️ Рекомендуется для личного использования (не для автопроверки ДЗ).

### 3. Автоматический запуск по расписанию

Запускает парсер каждый день в 19:00:

```bash
python -c "import scraper; scraper.run_scheduler()"


Для теста можно временно изменить время в коде:

```bash
schedule.every().day.at("21:42").do(job)


---

## 🧪 Автотесты

В проекте реализованы три теста (pytest):

Тест	Проверка
test_get_book_data_returns_dict	Возврат словаря с обязательными ключами
test_get_book_data_content	Корректность содержимого (название, цена, рейтинг)
test_scrape_books_creates_file	Создание файла books_data.txt и непустой контент

Запуск тестов:

```bash
pytest tests/test_scraper.py -v


Ожидаемый результат:

tests/test_scraper.py::test_get_book_data_returns_dict PASSED
tests/test_scraper.py::test_get_book_data_content PASSED
tests/test_scraper.py::test_scrape_books_creates_file PASSED


---

## 📦 Используемые библиотеки
Библиотека	Назначение
requests	HTTP-запросы
beautifulsoup4	Парсинг HTML
lxml	Быстрый HTML-парсер
schedule	Планировщик задач
pytest	Автоматическое тестирование
concurrent.futures	Многопоточность для ускорения

---

##👩‍💻 Автор

Ekaterina Kuchina
Московский Физико-Технический Институт (МФТИ)
2025 год
